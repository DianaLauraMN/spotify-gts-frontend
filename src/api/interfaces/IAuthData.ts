export interface IAuthData {
    access_token: string, refresh_token: string | null, expires_in: string, login_time: string, login_date: string, spoty_code: null | string, login_time_formated: string
} 